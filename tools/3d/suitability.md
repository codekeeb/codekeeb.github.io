# Reference suitability — verdict: CONDITIONAL
- Pass criteria met: one obvious target (a split keyboard), fills the frame, strong silhouette
  (plan outline of both halves), major materials visible (keycaps, plates, PCB, knob, OLED),
  approximable with procedural primitives (extruded outlines, rounded boxes, cylinders).
- Conditional because: single 3/4 front view only; underside/back hidden; keycap legends and
  exact keycap profile not required/available. Key positions come from authoritative layout data
  (js/geometria.js, CK_GEO.sofle), not from the photo, which removes the perspective problem.
- Accepted stylization: orientative, low-poly-ish browser prop; labelled on the page as
  "modelo 3D orientativo, no es una fotografía".
